import { SectionCard } from '@/components/section-card'; import { StatCard } from '@/components/stat-card'; import { requireAdminSession } from '@/lib/auth/admin'; import { listBookings } from '@/lib/services/rides'; import { listAllDriversAdmin } from '@/lib/services/accounts'; import { listTrips } from '@/lib/services/fleet';  export default async function AdminDashboardPage() {   await requireAdminSession();   const [bookings, trips, drivers] = await Promise.all([listBookings(), listTrips(), listAllDriversAdmin()]);    const activeTrips = trips.filter((trip) => trip.tripStatus !== 'completed' && trip.tripStatus !== 'cancelled');   const completedTrips = trips.filter((trip) => trip.tripStatus === 'completed');   const revenue = trips.reduce((sum, trip) => sum + trip.totalRevenue, 0);    const approvedDrivers = drivers.filter((driver) => driver.approvalStatus === 'approved' && driver.active && !driver.archivedAt);   const pendingDrivers = drivers.filter((driver) => driver.approvalStatus === 'pending');   const rejectedDrivers = drivers.filter((driver) => driver.approvalStatus === 'rejected');    return (     <div className="grid gap-4">       <div className="grid gap-3 md:grid-cols-2 xl:grid-cols-4">         <StatCard label="Tổng chuyến" value={String(bookings.length)} detail="Số chuyến đang ghi nhận." />         <StatCard label="Tài xế đã duyệt" value={String(approvedDrivers.length)} detail="Hiển thị cho khách hàng." />         <StatCard label="Tài xế chờ duyệt" value={String(pendingDrivers.length)} detail="Đang chờ xem xét." />         <StatCard label="Tài xế bị từ chối" value={String(rejectedDrivers.length)} detail="Không hiển thị cho khách hàng." />       </div>       <SectionCard title="Vận hành" description="Tổng quan đội xe và chuyến đi theo thời gian thực.">         <div className="grid gap-4 md:grid-cols-2">           <div className="rounded-2xl border border-white/10 bg-white/5 p-4 text-slate-200">             Chuyến đang chạy: {activeTrips.length}           </div>           <div className="rounded-2xl border border-white/10 bg-white/5 p-4 text-slate-200">             Chuyến đã hoàn thành: {completedTrips.length}           </div>           <div className="rounded-2xl border border-white/10 bg-white/5 p-4 text-slate-200">             Chuyến chờ xử lý: {bookings.filter((booking) => booking.status === 'pending').length}           </div>           <div className="rounded-2xl border border-white/10 bg-white/5 p-4 text-slate-200">             Doanh thu: {Math.round(revenue / 1000)}K           </div>         </div>       </SectionCard>     </div>   ); }