# Supabase Setup

1. Create a Supabase project.
2. Copy the values into `.env.local`.
3. Run `database/schema.sql` in the SQL editor.
4. Verify row-level security policies before exposing the app publicly.
5. Configure auth and notification workflows as the app evolves.
