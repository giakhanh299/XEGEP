'use client';  import { FormEvent, useState } from 'react'; import { useRouter } from 'next/navigation';  type AuthMode = 'login' | 'register'; type AuthRole = 'customer' | 'driver';  const customerInitial = {   username: '',   fullName: '',   phone: '',   address: '',   password: '' };  const driverInitial = {   username: '',   driverName: '',   phone: '',   vehicleType: '7-seat vehicle',   plateNumber: '',   seatCount: '7',   serviceArea: '',   vehiclePhoto: '',   driverPhoto: '',   description: '',   password: '' };  export function AuthForm() {   const router = useRouter();   const [mode, setMode] = useState<AuthMode>('login');   const [role, setRole] = useState<AuthRole>('customer');   const [customerForm, setCustomerForm] = useState(customerInitial);   const [driverForm, setDriverForm] = useState(driverInitial);   const [loginForm, setLoginForm] = useState({ username: '', password: '' });   const [message, setMessage] = useState<string | null>(null);   const [isSubmitting, setIsSubmitting] = useState(false);    async function handleSubmit(event: FormEvent<HTMLFormElement>) {     event.preventDefault();     setIsSubmitting(true);     setMessage(null);      try {       const payload =         mode === 'login'           ? loginForm           : role === 'customer'             ? {                 role,                 ...customerForm               }             : {                 role,                 ...driverForm,                 seatCount: Number(driverForm.seatCount)               };        const response = await fetch(mode === 'login' ? '/api/auth/login' : '/api/auth/register', {         method: 'POST',         headers: { 'Content-Type': 'application/json' },         body: JSON.stringify(payload)       });        const data = await response.json().catch(() => null);       if (!response.ok) {         throw new Error(data?.error ?? 'Không thể hoàn tất xác thực');       }        const nextPath = data?.user?.role === 'driver' ? '/driver' : '/customer';       router.push(nextPath);       router.refresh();     } catch (error) {       setMessage(error instanceof Error ? error.message : 'Đã xảy ra lỗi');     } finally {       setIsSubmitting(false);     }   }    return (     <form onSubmit={handleSubmit} className="glass space-y-5 rounded-[1.75rem] p-5">       <div className="flex flex-wrap gap-2">         {(['login', 'register'] as AuthMode[]).map((item) => (           <button             key={item}             type="button"             onClick={() => setMode(item)}             className={`rounded-2xl px-4 py-2 text-sm font-semibold ${               mode === item ? 'bg-emerald-400 text-slate-950' : 'bg-white/5 text-slate-200'             }`}           >             {item === 'login' ? 'Đăng nhập' : 'Đăng ký'}           </button>         ))}       </div>        {mode === 'register' ? (         <div className="flex flex-wrap gap-2">           {(['customer', 'driver'] as AuthRole[]).map((item) => (             <button               key={item}               type="button"               onClick={() => setRole(item)}               className={`rounded-2xl px-4 py-2 text-sm font-semibold ${                 role === item ? 'bg-cyan-400 text-slate-950' : 'bg-white/5 text-slate-200'               }`}             >               {item === 'customer' ? 'Khách hàng' : 'Tài xế'}             </button>           ))}         </div>       ) : null}        {mode === 'login' ? (         <div className="grid gap-3">           <label className="space-y-2 text-sm">             <span className="text-slate-200">Tên đăng nhập</span>             <input               required               value={loginForm.username}               onChange={(event) => setLoginForm((prev) => ({ ...prev, username: event.target.value }))}               className="w-full rounded-2xl border border-white/10 bg-slate-950/50 px-4 py-3 text-white outline-none focus:border-emerald-400/40"             />           </label>           <label className="space-y-2 text-sm">             <span className="text-slate-200">Mật khẩu</span>             <input               required               type="password"               value={loginForm.password}               onChange={(event) => setLoginForm((prev) => ({ ...prev, password: event.target.value }))}               className="w-full rounded-2xl border border-white/10 bg-slate-950/50 px-4 py-3 text-white outline-none focus:border-emerald-400/40"             />           </label>         </div>       ) : role === 'customer' ? (         <div className="grid gap-3 sm:grid-cols-2">           {[             ['username', 'Tên đăng nhập'],             ['fullName', 'Họ và tên'],             ['phone', 'Số điện thoại'],             ['address', 'Địa chỉ'],             ['password', 'Mật khẩu']           ].map(([key, label]) => (             <label key={key} className="space-y-2 text-sm sm:col-span-1">               <span className="text-slate-200">{label}</span>               <input                 required                 type={key === 'password' ? 'password' : 'text'}                 value={customerForm[key as keyof typeof customerForm]}                 onChange={(event) => setCustomerForm((prev) => ({ ...prev, [key]: event.target.value }))}                 className="w-full rounded-2xl border border-white/10 bg-slate-950/50 px-4 py-3 text-white outline-none focus:border-emerald-400/40"               />             </label>           ))}         </div>       ) : (         <div className="grid gap-3 sm:grid-cols-2">           {[             ['username', 'Tên đăng nhập'],             ['driverName', 'Tên tài xế'],             ['phone', 'Số điện thoại'],             ['plateNumber', 'Biển số xe'],             ['serviceArea', 'Khu vực hoạt động'],             ['vehiclePhoto', 'URL ảnh xe'],             ['driverPhoto', 'URL ảnh tài xế'],             ['description', 'Mô tả ngắn'],             ['password', 'Mật khẩu']           ].map(([key, label]) => (             <label key={key} className="space-y-2 text-sm sm:col-span-1">               <span className="text-slate-200">{label}</span>               <input                 required={key !== 'vehiclePhoto' && key !== 'driverPhoto' && key !== 'description'}                 type={key === 'password' ? 'password' : 'text'}                 value={driverForm[key as keyof typeof driverForm]}                 onChange={(event) => setDriverForm((prev) => ({ ...prev, [key]: event.target.value }))}                 className="w-full rounded-2xl border border-white/10 bg-slate-950/50 px-4 py-3 text-white outline-none focus:border-emerald-400/40"               />             </label>           ))}           <label className="space-y-2 text-sm">             <span className="text-slate-200">Loại xe</span>             <select               value={driverForm.vehicleType}               onChange={(event) => setDriverForm((prev) => ({ ...prev, vehicleType: event.target.value }))}               className="w-full rounded-2xl border border-white/10 bg-slate-950/50 px-4 py-3 text-white outline-none focus:border-emerald-400/40"             >               <option value="4-seat vehicle">Xe 4 chỗ</option>               <option value="7-seat vehicle">Xe 7 chỗ</option>             </select>           </label>           <label className="space-y-2 text-sm">             <span className="text-slate-200">Số ghế</span>             <select               value={driverForm.seatCount}               onChange={(event) => setDriverForm((prev) => ({ ...prev, seatCount: event.target.value }))}               className="w-full rounded-2xl border border-white/10 bg-slate-950/50 px-4 py-3 text-white outline-none focus:border-emerald-400/40"             >               <option value="4">4</option>               <option value="7">7</option>             </select>           </label>         </div>       )}        <button         type="submit"         disabled={isSubmitting}         className="w-full rounded-2xl bg-emerald-400 px-5 py-3 font-semibold text-slate-950 transition hover:bg-emerald-300 disabled:cursor-not-allowed disabled:opacity-70"       >               {isSubmitting ? 'Đang xử lý...' : mode === 'login' ? 'Đăng nhập' : role === 'customer' ? 'Tạo tài khoản khách hàng' : 'Tạo tài khoản tài xế'}       </button>        {message ? <p className="text-sm text-slate-300">{message}</p> : null}     </form>   ); }